name: Deploy Docs

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install docs dependencies
        run: pip install mkdocs-material jupyter nbconvert rustmc arviz matplotlib

      - name: Convert notebooks to markdown
        run: |
          jupyter nbconvert --to markdown --execute \
            --ExecutePreprocessor.timeout=300 \
            --output-dir docs/examples \
            docs/examples/linear-regression.ipynb \
            docs/examples/hierarchical.ipynb

      - run: mkdocs gh-deploy --force
